cat > payload.json << 'EOF'
{
  "query": "query ($search: String) { \
  Media(search: $search, type: ANIME) { \
id \
title { romaji english native } \
description(asHtml: false) \
coverImage { large } \
season \
seasonYear \
format \
genres \
tags { name isGeneralSpoiler } \
} \
}",
"variables": { "search": "ReawakeR" }
}
EOF
